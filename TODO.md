# TODO

1. 适配IEEE、PGLib及其他标准算例来源，使其复用当前标准化、映射、case和电力优化流程。
2. 支持case只选择部分spatial范围，并明确边界bus、跨区域输电支路、外部等值网络和功率交换约束。
3. 加强parameter数据源adapter、标准化与resolver的通用性，并补齐规划、UC/ED和潮流所需参数覆盖。
4. 在standard层把不同source的状态统一为规范值，例如`operating`、`construction`、`planned`和`retired`，使case层只需配置统一状态集合。
5. 进一步查验和细化parameter解析流程，包括候选匹配、优先级、冲突处理、聚合规则，以及是否生成并保存完整的validation报告。
6. 需要特别说明：manifest 中的 formula 当前是可审计的公式说明，并没有通过 eval 动态执行；实际计算仍由 PyPSA adapter 中的明确代码完成。这种方式更安全，但后续最好增加受控的 transform 注册机制，进一步避免公式说明与代码实现不一致。
7. 当前这 25 条契约基本覆盖现有网络转换和简化运行模拟，但如果启用完整整数 UC、容量扩建、备用约束或多阶段规划，还需要继续补充启停成本、最小启停时间、资本成本、寿命和折现率等契约。
8. standard中network相关模块可否合并，其中代码进行压缩，这样保持文件组织简洁清爽。
9. 没有水库水电、混合式水电和光热的时序模型。
10. 为完整500 kV网络的日级及更长时域仿真增加可校验的滚动时域和网络降阶方案；当前默认4小时窗口用于保证示例可在合理时间内达到最优。
11. standard处理最终形成的network还是要检查